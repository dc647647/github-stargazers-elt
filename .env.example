# Default GitHub token — used as fallback if a repo-specific token is not set.
# Generate tokens at https://github.com/settings/tokens (no special scopes needed for public repos)
GITHUB_TOKEN=your_token_here

# Per-repo tokens — each has its own 5,000 req/hr rate limit budget.
# Allows full parallelism across all 5 Airflow tasks without rate limit contention.
GITHUB_TOKEN_DBT_CORE=your_token_here
GITHUB_TOKEN_AIRFLOW=your_token_here
GITHUB_TOKEN_DAGSTER=your_token_here
GITHUB_TOKEN_DUCKDB=your_token_here
GITHUB_TOKEN_DLT=your_token_here

# Path to the DuckDB database file
# Defaults to ./data/stargazers.duckdb relative to the project root
# DUCKDB_PATH=/absolute/path/to/stargazers.duckdb
