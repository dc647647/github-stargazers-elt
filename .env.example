# GitHub personal access token (strongly recommended)
# Without it you get 60 unauthenticated requests/hr — not enough for large repos.
# Generate one at https://github.com/settings/tokens (no special scopes needed for public repos)
GITHUB_TOKEN=your_token_here

# Path to the DuckDB database file
# Defaults to ./data/stargazers.duckdb relative to the project root
# DUCKDB_PATH=/absolute/path/to/stargazers.duckdb
